[package]
name = "part_2"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
haversine = { path = "haversine" }
haversine_gen = { path = "haversine_gen" }
json_parser = { path = "json_parser" }
